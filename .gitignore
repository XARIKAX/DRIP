# deps
node_modules/
.pnpm-store/

# next
.next/
out/
next-env.d.ts

# builds
dist/
build/
*.tsbuildinfo

# `build/` is unanchored, so it also matches app router segments named build. Re-include
# the directory itself — git cannot re-include a file whose parent directory is excluded.
!apps/web/src/app/build/

# foundry
contracts/out/
contracts/cache/
contracts/broadcast/
# forge writes the invariant corpus next to the invocation, not the project root
cache/

# env
.env
.env.local
.env.*.local

# misc
.DS_Store
*.log
coverage/
