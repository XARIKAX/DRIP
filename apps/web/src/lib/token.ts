/**
 * The token itself.
 *
 * One constant, because an address that appears in two places eventually appears in
 * two forms, and the cost of that mistake is somebody's money. Stored in EIP-55
 * checksummed form: the mixed case is a checksum, so a wallet can reject a typo that
 * an all-lowercase address would accept.
 */
export const OSINKO_ADDRESS = "0x8cd57b19033A90b753d73DBc122367A4A52B8586";

export const OSINKO_SYMBOL = "$OSINKO";
