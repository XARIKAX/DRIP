import { Docs } from "@/components/docs/Docs";

/**
 * The documentation. One page, numbered like a prospectus, read top to bottom or
 * jumped into from the contents: what Osinko is, how each module works, what can go
 * wrong, and every number a contract enforces. Metadata lives in the layout so this
 * file stays a single line of composition.
 */
export default function DocsPage() {
  return <Docs />;
}
